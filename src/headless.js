/* import puppeteer from './variables.js'

export default headless = (async () => {
    const browser = await puppeteer.launch({ headless: false });
	const page = await browser.newPage();
	await page.goto('http://localhost:3000');
})(); */